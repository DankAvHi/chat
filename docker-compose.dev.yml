services:
    frontend:
        restart: always
        build:
            context: chat-client
            dockerfile: Dockerfile.dev
        volumes:
            - ./chat-client:/usr/app/
            - /usr/app/node_modules
        ports:
            - "3000:3000"
