services:
    frontend:
        restart: always
        build:
            context: client
            dockerfile: Dockerfile.dev
        volumes:
            - ./client:/usr/app/
            - /usr/app/node_modules
        ports:
            - "3000:3000"
